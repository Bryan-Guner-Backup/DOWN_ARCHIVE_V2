import OktaApolloProvider from './OktaApolloProvider';
import { cleanup, render, wait } from "@testing-library/react";

const onSubmit = jest.fn();

describe('OktaApolloProvider', () => {
  beforeEach(cleanup);
  afterEach(cleanup);
  
  it('should set okta token', async () => {
    
  });
  
})