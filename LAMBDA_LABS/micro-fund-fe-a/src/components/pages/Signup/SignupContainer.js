import React from 'react';
import RenderSignupPage from './RenderSignupPage';

function SignupContainer({ LoadingComponents }) {
  return (
    <>
      <RenderSignupPage />
    </>
  );
}

export default SignupContainer;
